allprojects {
    repositories {
        mavenCentral()
    }
}

tasks.register("run") {
    dependsOn(":checkout:bootRun")
}

tasks.register("test") {
    dependsOn(":offer:test", ":pricing:test", ":checkout:test")
}